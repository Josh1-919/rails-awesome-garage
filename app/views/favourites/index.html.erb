<h1>My Garage</h1>
<hr>
<% @favourites.each do |favourite| %>
  <h3> <%= favourite.car.brand %></h3>
  <p> <%= favourite.car.model %></p>
  <%= link_to "Delete", favourite_path(favourite), data:{turbo_method: :delete, turbo_confirm: "Are you sure to delete #{favourite.car.model}?"} do %>
  <% end %>
  <hr>
<%end%>
